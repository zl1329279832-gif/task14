# 拓扑监控前端生命周期重构 — 测试复现指南

## 修复的问题清单

| # | 问题 | 根因 | 修复位置 |
|---|------|------|----------|
| 1 | 导入新拓扑后旧告警继续播放 | `loadData` 未停止旧 `AlertReplay` 定时器 | `app.js` → `AlertReplay.reset()` |
| 2 | 旧节点选中状态带入新图 | `interaction.js` 闭包内 `selectedNodes` 未清空 | `interaction.js` → `resetState()` |
| 3 | 旧折叠分组状态带入新图 | `restoreLayout` 无签名校验，盲目恢复旧折叠 | `import-export.js` → 签名比对 |
| 4 | 旧上下游高亮带入新图 | `clearHighlights()` 未在新数据加载前调用 | `interaction.js` → `resetState()` |
| 5 | 布局缓存跨拓扑复用 | `restoreLayout` 只按节点ID匹配，无拓扑签名 | `data-parser.js` → `computeTopologySignature()` |
| 6 | 乱序告警排序不稳定 | 相同时间戳的告警排序不确定 | `data-parser.js` → `localeCompare` 二级排序 |
| 7 | 重复告警ID导致状态混乱 | 未检测重复 `alert.id` | `data-parser.js` → 告警ID去重 |
| 8 | 孤立节点未被标记 | 已有检测但告警引用不存在的节点时崩溃 | `data-parser.js` → 引用校验 |
| 9 | 环路链路未阻断 | 已有检测但反向重复连线未处理 | `data-parser.js` → `reverseKey` 检查 |
| 10 | 分组折叠后搜索定位失效 | 点击搜索结果时不展开折叠分组 | `interaction.js` → 搜索点击自动展开 |
| 11 | 搜索与告警高亮互相清除 | `clearHighlights()` 同时清除搜索和告警高亮 | `alert-replay.js` → 独立高亮追踪集合 |
| 12 | 缩放拖拽后告警链路高亮偏移 | diff-renderer 字体/徽章/粒子/波前未除以 `viewport.scale` | `diff-renderer.js` → 统一缩放 |
| 13 | 筛选后依赖链重算不准 | `_applyDiffFilters` 未更新传播链可见性 | `app.js` → 链可见性标记 |
| 14 | 点击告警后视口不跟踪节点 | `seekTo` / `_seekToIndex` 未居中视口到告警节点 | `alert-replay.js` + `fault-replay.js` → 视口居中 |
| 15 | 刷新后节点不可点击 | `loadData` 中 `fitToView` 覆盖了 `restoreLayout` 恢复的视口 | `app.js` → 条件性 `fitToView` |
| 16 | JSON 导出不含视口状态 | `exportData` 缺少 `viewport` 字段 | `import-export.js` → 导出视口 |
| 17 | diff 导出缺少节点坐标 | `getSerializableResult` 剥离了 x/y | `topo-diff.js` → 保留坐标 |
| 18 | diff 布局恢复后视口被覆盖 | `loadDiffData` 中 `fitToView` 无条件调用 | `app.js` → 条件性 `fitToView` |
| 19 | 高亮应用到已筛选隐藏的节点 | `_applyHighlightsToNodes` 不检查 `_visible` | `fault-replay.js` → 可见性检查 |
| 20 | 视口恢复缺少有效性校验 | `restoreLayout` 对 `NaN` / `Infinity` 未防护 | `import-export.js` → `isFinite` 校验 |

## 复现测试步骤

### 测试1: 导入新拓扑清空旧状态

1. 打开应用，点击 **"示例"** 加载默认拓扑
2. 播放告警到第5步（点击 ▶️ 或拖动滑块），观察节点高亮和状态变化
3. 选中几个节点（Shift+点击），折叠 "北京" 分组
4. 在搜索框输入 "API" 并观察高亮
5. **不暂停回放**，直接导入 `test-topology-a.json`
6. **验证**：
   - [x] 回放已停止，时间线重置为 `--:--:--`
   - [x] 无旧节点选中蓝环
   - [x] 无旧搜索高亮
   - [x] 无旧告警状态（所有节点为 normal）
   - [x] 所有分组已展开（无折叠状态残留）
   - [x] 搜索框已清空

### 测试2: 布局缓存签名校验

1. 加载 `test-topology-a.json`，手动拖动几个节点到特定位置，点击 **"保存"**
2. 刷新页面 → **验证**：节点恢复到保存位置
3. 导入 `test-topology-b.json`（有同名节点 `srv-1`, `srv-2`, `db-1`）
4. **验证**：
   - [x] 节点不沿用 A 拓扑的位置（自动布局）
   - [x] 控制台输出 `🔄 拓扑签名不匹配`
5. 重新加载 `test-topology-a.json`
6. **验证**：节点恢复到步骤1保存的位置（签名匹配）

### 测试3: 乱序告警和重复ID

1. 导入 `test-topology-a.json`
2. 观察验证对话框
3. **验证**：
   - [x] 提示"时间乱序"告警已按时间戳正确排序
   - [x] 提示"同时间戳"告警已按ID稳定排序
   - [x] 提示"引用不存在的节点 ghost-node"
   - [x] 提示"引用不存在的连线 nonexistent_link_id"
   - [x] 提示"受影响节点 also-ghost 不存在，已移除"
   - [x] 回放时不因空引用崩溃

### 测试4: 孤立节点和环路

1. 导入 `test-topology-a.json`
2. **验证**：
   - [x] 提示"1 个孤立节点: orphan-1"
   - [x] 提示"检测到环形依赖"
   - [x] 提示"重复连线" 和 "反向重复" 被跳过
   - [x] 提示源/目标不存在的连线被跳过

### 测试5: 分组折叠后搜索定位

1. 导入 `test-topology-c.json`
2. 折叠 "接入层" 分组（双击分组区域）
3. 搜索 "APP前端" 并点击搜索结果
4. **验证**：
   - [x] "接入层" 分组自动展开
   - [x] "APP前端" 节点被选中并居中显示
   - [x] 节点详情面板弹出

### 测试6: 搜索与告警高亮互不干扰

1. 导入 `test-topology-c.json`
2. 搜索 "数据库" → 观察 `c-db1`, `c-db2` 高亮（虚线蓝环）
3. 播放告警到第2步（`c-srv1` 宕机）
4. **验证**：
   - [x] 搜索框仍显示 "数据库"
   - [x] `c-db1`, `c-db2` 仍有搜索高亮
   - [x] `c-srv1` 有告警高亮
   - [x] 两种高亮可以共存

### 测试7: 告警严重度只升不降

1. 导入 `test-topology-a.json`
2. 播放到第1步：`srv-2` 变为 `critical`
3. 继续播放到最后一步：`srv-2` 收到 `normal` 恢复告警
4. **验证**：
   - [x] `srv-2` 状态仍为 `critical`（不降级）

### 测试8: 缩放拖拽后告警高亮不偏移

1. 点击 **"示例"** 加载默认拓扑
2. 使用鼠标滚轮缩放至 **200%**
3. 拖拽画布使拓扑偏移到左下角
4. 点击告警事件列表中的第3条告警（`MySQL主库连接数接近上限`）
5. **验证**：
   - [x] `db-a1` 节点上的高亮环（蓝色虚线圆）与节点形状完全对齐，无偏移
   - [x] 相关连线高亮（蓝色实线）精确覆盖在源/目标节点之间的连线上
   - [x] 视口自动居中到 `db-a1` 节点（如果它原本不在可视区域内）
6. 缩小至 **50%**，拖拽到右上角
7. 点击第5条告警（`支付系统服务不可用`）
8. **验证**：
   - [x] `app-pay` 节点高亮位置正确，脉冲动画居中在节点上
   - [x] 连线高亮不偏离真实节点位置

### 测试9: Diff 模式缩放拖拽后回放

1. 进入差异对比模式，导入 `test-topology-a.json`（快照A）、`test-topology-b.json`（快照B）、`test-alerts.json`
2. 缩放至 **150%**，拖拽画布
3. 点击 **"传播"** 按钮单步回放
4. **验证**：
   - [x] 传播波前同心环以告警节点为中心，无偏移
   - [x] 传播链上的流动粒子精确沿连线移动
   - [x] 变更节点的 marching ants 边框与节点形状对齐
5. 继续点击 **"链"** 跳到下一条传播链
6. **验证**：
   - [x] 高亮的链路连线精确覆盖在节点间连线上
   - [x] 受影响业务节点的紫色光晕居中在节点上
7. 缩放至 **30%**（远视），验证所有高亮元素仍正确对齐

### 测试10: 筛选后依赖链重算

1. 在 diff 模式下，取消勾选 **"不变"** 状态筛选
2. **验证**：
   - [x] 状态栏显示"传播链: N 条"，只统计经过可见节点的链
   - [x] 不变节点被隐藏，传播链中经过隐藏节点的步骤不计入
3. 取消勾选 **"删除"** 状态筛选
4. 点击告警列表中的告警
5. **验证**：
   - [x] 被过滤掉的节点不显示高亮
   - [x] 高亮只出现在当前可见的节点上
   - [x] 不会在空白区域看到"幽灵高亮"

### 测试11: localStorage 恢复后节点可点击

1. 加载示例数据，手动拖拽几个节点到不同位置
2. 缩放至 **180%**，拖拽画布到特定位置
3. 播放告警到第4步
4. 点击 **"保存"** 按钮
5. **刷新页面**
6. **验证**：
   - [x] 节点恢复到保存的位置（不是自动布局位置）
   - [x] 视口恢复到 **180%** 缩放和之前的偏移
   - [x] 所有节点可以点击（点击后有蓝色选中环）
   - [x] 可以拖拽节点（拖拽时节点跟随鼠标）
   - [x] 右键菜单正确定位在鼠标位置
7. 进入差异对比模式，导入数据，缩放拖拽后保存
8. **刷新页面**
9. **验证**：
   - [x] 差异模式提示恢复
   - [x] 点击恢复后，节点位置和视口都正确恢复
   - [x] 回放进度恢复到之前的告警步骤

### 测试12: JSON 导出包含正确坐标

1. 加载示例数据，缩放至 **250%** 并拖拽
2. 点击 **"导出"**
3. 打开导出的 JSON 文件
4. **验证**：
   - [x] `nodes` 中每个节点包含世界坐标 `x`、`y`（整数）
   - [x] `viewport` 字段包含当前视口 `x`、`y`、`scale`
   - [x] 导出的坐标与画布上显示的一致（导入后位置不变）
5. 在 diff 模式下导出差异结果
6. **验证**：
   - [x] `mergedTopology.nodes` 包含每个节点的世界坐标
   - [x] `mergedTopology.links` 使用节点 ID 引用（非对象引用）
   - [x] `viewport` 包含当前视口状态

### 测试13: minimap 视口指示器与主画布同步

1. 加载示例数据
2. 缩放至 **200%**
3. 观察小地图中的视口矩形
4. **验证**：
   - [x] 视口矩形覆盖的区域与主画布显示的区域一致
   - [x] 拖拽主画布时，小地图视口矩形同步移动
5. 缩放至 **50%**
6. **验证**：
   - [x] 视口矩形变大（显示更多世界区域）
   - [x] 视口矩形边界与小地图中节点的相对位置正确

## 文件变更清单

```
js/data-parser.js    +42行  拓扑签名计算、告警去重、引用校验、稳定排序
js/alert-replay.js   +49行  reset()方法、独立高亮追踪集合、告警点击居中视口
js/interaction.js    +65行  resetState()方法、搜索自动展开折叠分组、diff告警点击居中
js/import-export.js  +58行  布局签名保存与校验、视口状态验证、diff导出含坐标、播放进度保存视口
js/app.js            +22行  loadData()前调用reset、signature状态字段、视口恢复保护、筛选链重算
js/diff-renderer.js  +40行  统一viewport缩放：字体、徽章、粒子、波前、lineWidth
js/fault-replay.js   +18行  高亮应用时尊重节点可见性、回放seek居中视口
js/topo-diff.js      +2行   序列化结果包含节点坐标
test-topology-a.json        边界场景测试：乱序、重复、孤立、环路
test-topology-b.json        跨拓扑签名测试：同名节点不同拓扑
test-topology-c.json        折叠+搜索+回放干扰测试
```
